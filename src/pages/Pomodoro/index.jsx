import { Container } from './styles';
import { Header } from '../../components/Header';

export const PomodoroComponent = () => {
    

    return (
        <Container>
            <Header />
            Pomodoro
        </Container>
    );
}